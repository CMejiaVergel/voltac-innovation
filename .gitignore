node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# Cualquier variante de .env queda fuera, incluidas las copias de seguridad
# tipo .env.bak-20260829: llevan SESSION_SECRET y claves de API dentro.
.env
.env.*
!.env.example

prisma/*.db
prisma/*.db-journal
prisma/*.db-wal
prisma/*.db-shm
# Solo el directorio de datos en la raiz. `prisma/data/` SI se versiona: ahi
# vive el contenido semilla que necesita `npm run seed`.
/data/

.DS_Store
Thumbs.db
npm-debug.log*
.pm2/

# Token del servidor MCP: nunca al repositorio
.mcp.local.json
